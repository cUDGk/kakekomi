fix(post-p8): バグ 1-4 を塞ぐ + 検証

1. HEIC を default/safe MIME から除外
   - 旧: image/heic を allow_mime に入れていたが StripImageMetadata に case なし → メタ素通り
   - 新: safeMIMEs と DefaultConfig.AllowedMIME 両方から削除、コメントで理由明記
   - iPhone ユーザは JPEG/PNG に変換してから投稿する運用

2. CSP から 'unsafe-inline' 削除
   - CSS を layout.html の inline から templates/style.css に分離
   - /static/style.css ハンドラを追加 (embed.FS から配信、独自に Content-Type: text/css)
   - CSP: style-src 'self' (unsafe-inline なし)
   - 検証: CSP ヘッダから unsafe-inline 消失 + /static/style.css = 200/2016 bytes

3. /reply timing leak 解消 (O(N) → O(1))
   - cases.code_lookup BLOB 列 + INDEX を追加
   - code_lookup = HMAC-SHA256(secrets.LookupKey, code), domain label = "kakekomi/v1/case-code-lookup"
   - secrets.age に LookupKey (32B random) を追加
   - FindCaseByCode: SELECT WHERE code_lookup = ? LIMIT 1 → 0 or 1 row
   - 1 row の場合のみ argon2id 検証、0 row でも dummy argon2id で時間を均す
   - 検証 (6 cases / 5x bulk submit 後):
       wrong: 1508/1507/1503 ms
       right: 1507 ms (差 5 ms 以内、件数非依存)

4. 匿名 CSRF を PoW token から独立
   - 旧: CSRF = HMAC(csrf_key, "anon-csrf" || pow_token) → PoW を取った時点で CSRF も自作可能
   - 新: GET /submit で random 32B トークンを生成、cookie + form field の double-submit
   - Cookie: kakekomi_anon_csrf (HttpOnly, SameSite=Strict, Path=/submit, Secure-conditional)
   - POST 時に cookie 値 == form 値を constant-time 比較
   - 検証: cookie と _csrf が同一値で送信成功、_pow は完全独立な値

副次の追加:
- crypto.go: CaseCodeLookup() 関数 + HMACCaseCodeLookup domain label
- secrets.go: LookupKey フィールド + NewSecrets() で生成
- store.go: code_lookup INSERT/SELECT + dummy argon2id でタイミング均一化
- session.go: NewAnonCSRFToken / VerifyAnonCSRF / setAnonCSRFCookie / readAnonCSRFCookie
- templates/style.css: 元の inline CSS を移植 (+ input[type=file] と .required スタイル)
- templates/layout.html: <link rel="stylesheet" href="/static/style.css">
- handlers.go: renderPublic シグネチャに r *http.Request と anonCSRF を追加、全コールサイト更新

E2E 再確認:
✓ /submit GET → anon CSRF cookie + form _csrf が同一、_pow と独立
✓ /submit POST → 投稿成功
✓ /reply 6 cases で wrong/right とも 1500ms±5ms (件数非依存)
✓ /static/style.css 配信
✓ HEIC は config validate で reject (allow_mime に入れたら起動エラー)
