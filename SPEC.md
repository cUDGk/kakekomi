# kakekomi 仕様書

## 1. 製品コンセプト

個人ジャーナリスト/弁護士/監査人が **30 分以内** に自前の **匿名通報窓口** を立てるための、Go 単一バイナリ製 OSS。

ゴール: 「シンプルにセキュリティを高く」。機能を足さず、削る事で攻撃面を最小化する。

## 2. 設計原則

1. **削減主義**: 機能を足すよりも削る。攻撃面の最小化こそ最大のセキュリティ機能。
2. **デフォルトで最強**: 設定を弄らずとも安全に動く。弱める方向のみオプトイン。
3. **書かない情報は漏れない**: ログ・IP・UA・Referer などを一切記録しない。
4. **平文を残さない**: ディスク上の通報内容はすべて age 暗号化済み。
5. **シンプルな UI**: 通報者画面は 3 つだけ。装飾なし、外部リソースゼロ。
6. **項目は受信者が自由定義**: フォームは config 駆動、全項目 optional がデフォルト。

## 3. 機能要件

### 3.1 通報受信 (MUST)

| ID | 要件 |
|---|---|
| F-01 | Tor Hidden Service v3 経由で通報フォームを公開できる |
| F-02 | 受信した通報内容は age 暗号化してディスクに保存する |
| F-03 | 通報者は diceware 7 語のコードを受け取り、再訪・追加情報送信・返信受信できる |
| F-04 | ファイル添付に対応 (デフォルト 10 ファイル / 各 200 MB / 合計 500 MB) |
| F-05 | 添付の EXIF / XMP / ICC / Office / PDF メタを除去する |
| F-06 | 通報 1 件ごとに PoW (hashcash 型) を要求 |
| F-07 | TTL を超えた通報は自動削除される |
| F-08 | 通報者にも age 鍵ペアを発行 (双方向 E2E) |

### 3.2 設定 (MUST)

| ID | 要件 |
|---|---|
| F-10 | フォーム項目は `kakekomi.yaml` で受信者が自由定義できる |
| F-11 | 全項目のデフォルトは optional (`required: true` 明示時のみ必須) |
| F-12 | デフォルト設定では「通報内容」フィールドのみ必須 |
| F-13 | 項目タイプ: text / textarea / select / date / checkbox / number |
| F-14 | `kakekomi config validate` で構文・論理エラーを検出 |
| F-15 | 項目の追加・削除・並び替えがすべて可能 |

### 3.3 受信者ダッシュボード (MUST)

| ID | 要件 |
|---|---|
| F-20 | 別の .onion v3 (Client Auth 付き) で公開 |
| F-21 | ログインはパスワード + TOTP |
| F-22 | 通報一覧は ID / 受信日時 / サイズのみ表示 (本文はサーバが復号不能) |
| F-23 | 暗号化 blob (.tar.age) を DL できる |
| F-24 | 受信者がローカルで暗号化した返信 blob をアップロードできる |
| F-25 | 通報の削除を手動で行える |

### 3.4 鍵管理 (MUST)

| ID | 要件 |
|---|---|
| F-30 | `kakekomi init` で age 鍵ペアを生成 |
| F-31 | 秘密鍵はサーバに保存しない (init 時に受信者に提示、即削除) |
| F-32 | 鍵ローテーションコマンドあり (旧 blob は旧鍵でアクセス可能) |
| F-33 | 通報者用の鍵ペアも /submit 直後に DL 可能 |

### 3.5 CLI (MUST)

| ID | コマンド |
|---|---|
| F-40 | `kakekomi init` |
| F-41 | `kakekomi run` |
| F-42 | `kakekomi config validate` |
| F-43 | `kakekomi rotate-key` |
| F-44 | `kakekomi gc` |
| F-45 | `kakekomi version` |

### 3.6 やらない事 (MUST NOT)

| ID | 禁止事項 |
|---|---|
| F-X1 | IP アドレスをログに残さない |
| F-X2 | UserAgent / Referer を記録しない |
| F-X3 | アクセスログを一切出力しない (運用ログは最小化、エラーログのみ stderr) |
| F-X4 | 外部 CDN / Web フォント / GA / 解析ツールを読み込まない |
| F-X5 | 通報内容をサーバ上で復号しない (UI に「閲覧」ボタンを作らない) |
| F-X6 | 通報者の Cookie / LocalStorage を残さない (Session は in-memory のみ) |
| F-X7 | 受信者の秘密鍵をサーバに保存しない |

### 3.7 SHOULD

| ID | 要件 |
|---|---|
| S-01 | i18n: 日本語 / 英語 (v1) |
| S-02 | Honeypot フィールド対応 (ボット検出) |
| S-03 | 受信者向け通知 (Tor 経由 webhook のみ、平文通知は禁止) |
| S-04 | 添付ファイル名の自動ハッシュ化 |
| S-05 | 再現可能ビルド (GitHub Actions) |

### 3.8 MAY (将来)

| ID | 要件 |
|---|---|
| M-01 | 多受信者 (組織内の複数ジャーナリストで受信) |
| M-02 | 通報者⇔受信者の双方向リアルタイムチャット |
| M-03 | SaaS 版 (運営型) — ★ 別レイヤとして開発、本リポでは扱わない |
| M-04 | 公益通報者保護法準拠の証跡管理モード (別プロダクト) |

## 4. UI 仕様

### 4.1 通報者画面 (公開 .onion)

#### `/` (トップ)

```
┌────────────────────────────────────────┐
│                                        │
│  通報窓口                              │
│                                        │
│  [config.site.intro の本文]            │
│                                        │
│  [ 通報を始める ]                      │
│                                        │
│  すでにコードをお持ちの方 → /reply     │
│                                        │
└────────────────────────────────────────┘
```

- HTML 静的、JS なし

#### `/submit` (送信フォーム)

- config.yaml の `fields` を動的レンダリング
- `required: true` の項目のみアスタリスク
- 送信時の挙動:
  1. ブラウザ側で PoW 計算 (進捗バー)
  2. POST 送信
  3. /done へ遷移
- JS は `static/pow.js` のみ。外部読み込み一切なし

#### `/done` (完了)

```
┌────────────────────────────────────────┐
│  ✓ 通報を受け付けました                │
│                                        │
│  あなたのコード:                       │
│  ┌────────────────────────────────┐    │
│  │ ocean·marble·pencil·ladder·    │    │
│  │ thunder·hollow·spring          │    │
│  └────────────────────────────────┘    │
│                                        │
│  ⚠ このコードを紙にメモして保管して    │
│    ください。サーバには保存されません。 │
│    紛失すると追加通報・返信受信が      │
│    できなくなります。                  │
│                                        │
│  [ 自分用の鍵をダウンロード ]          │
│   (返信受信に必要)                     │
│                                        │
└────────────────────────────────────────┘
```

#### `/reply` (再訪)

- コード入力欄 1 つ
- 入力後:
  - 受信者からの返信があれば暗号化 blob を DL リンクで提示
  - 追加通報フォーム (`/submit` と同じ動的フォーム) も同居

### 4.2 受信者画面 (.onion v3 + Client Auth)

#### `/admin` ログイン

- パスワード + TOTP のみ
- ログイン失敗時は **理由を返さない** (アカウント存否を漏らさない)

#### `/admin/inbox` 受信箱

```
┌───────────────────────────────────────────────────┐
│ ID         受信日時              サイズ   状態    │
├───────────────────────────────────────────────────┤
│ a3f9...   2026-05-15 09:22     1.2 MB    未読     │
│ 7c01...   2026-05-14 22:11     420 KB    既読     │
│ ...                                               │
└───────────────────────────────────────────────────┘
```

- 本文プレビューなし (サーバは平文を知らないため)
- ID クリック → `/admin/case/:id`

#### `/admin/case/:id`

- メタ情報 (受信時刻、サイズ、TTL) のみ表示
- 「暗号化 blob (.tar.age) を DL」 ボタン
- 大きい警告: 「★ このサーバ上で開かないこと。手元の端末で `age -d -i secret.age receiver.age` してください」

#### `/admin/reply/:id`

- ローカルで age 暗号化した返信 blob をアップロード
- 受信者のローカルでの暗号化手順を表示:
  ```
  age -r <通報者の公開鍵> -o reply.age reply.txt
  ```

### 4.3 デザイン規約

- カラー: 背景 `#fafafa` / 文字 `#1a1a1a` / アクセント `#1f6feb` (1 色のみ)
- フォント: `font-family: ui-sans-serif, system-ui, -apple-system, sans-serif` のみ
- 角丸: なし or 2px 固定
- アニメーション: なし
- 影: なし
- レスポンシブ: mobile-first, `max-width: 720px`
- ダークモード: v1 では未対応 (固定テーマで指紋を減らす)

## 5. 設定ファイル仕様

### 5.1 必須キー

- `receiver.public_key` (init で自動投入)
- `tor.ingress_onion_dir`

### 5.2 デフォルト動作

すべての設定にコード内デフォルトあり。`kakekomi.yaml` が無くても `kakekomi init` で雛形が生成される。

### 5.3 検証

- `kakekomi config validate` でスキーマ + 論理検証
- 危険な設定 (例: ログ有効化、PoW 無効化、retention 無限) は警告

### 5.4 ホットリロード

- v1 では未対応 (config 変更時は `kakekomi run` を再起動)
- 理由: 設定変更タイミングが Tor 接続維持と相性が悪い

## 6. 非機能要件

- 起動時間: < 2 秒 (Tor 起動含むと別途 + 20〜60 秒)
- メモリ: < 100 MB アイドル時
- 配布: 単一バイナリ
  - linux/amd64
  - linux/arm64
  - darwin/arm64
  - windows/amd64
- 依存ランタイム: なし

## 7. 監査・配布

- 第三者監査を受けるまで `v1.0.0` を切らない
- リリースバイナリは GitHub Actions で再現可能ビルド
- SBOM (CycloneDX) を release artifact に同梱
- リリースタグは GPG 署名
- 全リリースに SHA-256 を README とリリースノートの両方に併記

## 8. ライセンス

**AGPL-3.0**

- SecureDrop 精神の継承
- SaaS 化による囲い込みを防止 (改変版をネットワーク経由で提供する場合もソース公開義務)
- フォークしての商用利用は可、ただし改変分も AGPL-3.0 で公開する必要あり

## 9. 開発フェーズ (再掲)

- **Phase 0** (現在): 設計文書 (THREAT_MODEL / ARCHITECTURE / SPEC)
- **Phase 1**: 骨組み実装 — `init` + `run` + テキスト通報のみ (Tor 統合前、localhost で動作)
- **Phase 2**: 添付 + メタデータ除去
- **Phase 3**: Tor 統合 (go-libtor 同梱)
- **Phase 4**: 受信者ダッシュボード + 通報コード再訪
- **Phase 5**: PoW + retention + GC
- **Phase 6**: 受信者⇔通報者の双方向 E2E
- **Phase 7**: RC + 第三者監査依頼
- **Phase 8**: v1.0.0 リリース

## 10. リポジトリ構成 (予定)

```
kakekomi/
├── THREAT_MODEL.md
├── ARCHITECTURE.md
├── SPEC.md
├── README.md             # まだ書かない (実装前にハイプを作らない)
├── LICENSE               # AGPL-3.0
├── go.mod
├── go.sum
├── cmd/
│   └── kakekomi/main.go
├── internal/
│   ├── server/           # HTTP layer
│   ├── crypto/           # age wrapper
│   ├── store/            # SQLite + blob
│   ├── tor/              # go-libtor wrapper
│   ├── stripper/         # メタデータ除去
│   ├── pow/              # hashcash
│   └── config/           # YAML loader + validator
├── web/
│   ├── templates/
│   └── static/
│       └── pow.js
├── examples/
│   └── kakekomi.example.yaml
└── .github/
    └── workflows/
        └── release.yml   # 再現可能ビルド + SBOM
```
