# kakekomi 脅威モデル

## 0. 位置づけ
kakekomi は SecureDrop の代替ではない。個人ジャーナリスト/弁護士/監査人向けの **軽量匿名通報窓口** であり、本家 SecureDrop の air-gapped viewing station / Tails OS 統合等は提供しない。

本家との明確な違い:
- 単一 Go バイナリで完結 (Python + 専用OSスタックなし)
- viewing station を持たず、受信者の通常端末で復号する事を許容
- 「30分でセットアップ」を可能にする代わりに、脅威モデルは個人〜小規模運用に限定

## 1. 守るべき資産

| 優先度 | 資産 | 失われた場合 |
|---|---|---|
| P0 | 通報者の身元 (IP、デバイス情報、書き方の癖) | 命・職を失う |
| P0 | 通報内容の機密性 | 通報元の特定/報復、二次被害 |
| P1 | 通報者⇔受信者の通信タイミング | 相関攻撃の材料 |
| P1 | 通報の存在自体 | 「通報されている事実」のリーク |
| P2 | サーバの可用性 | 一時的に通報不能 |

## 2. 想定敵 (in scope)

### A1: サーバ物理押収者
- 想定主体: 警察/差し押さえ、ホスティング会社内部者、サーバ管理権を奪った攻撃者
- 能力: ディスク全コピー、稼働中ならメモリダンプ、SQLite フル取得
- 防御:
  - 通報本文・添付は **age envelope encryption** でディスクに書く前に暗号化
  - 受信者の **秘密鍵はサーバに置かない**
  - SQLite には暗号化 blob への参照と通報コードのハッシュ (argon2id) のみ
  - メモリ上の平文は処理完了後に zero 化

### A2: ネットワーク傍受者
- 想定主体: ISP、Wi-Fi 盗聴、国家レベルの受動観測
- 能力: clearnet TLS の外側メタデータ、Tor 入口/出口の片側観測
- 防御:
  - **Tor Hidden Service v3 必須** (clearnet ingress を持たない)
  - 受信者ダッシュボードも別の .onion + Client Authorization

### A3: 受信者アカウント乗っ取り
- 想定主体: パスワード窃取・フィッシング・端末乗っ取り
- 能力: 受信者ダッシュボードに正規ログイン
- 防御:
  - ダッシュボードにログインできても **秘密鍵がサーバに無いため復号不能**
  - 攻撃者が得られるのは暗号化 blob のみ
  - パスワード + TOTP + Tor Client Auth の三重防御

### A4: 悪意ある通報者
- 想定主体: DDoS、ストレージ枯渇、デマ大量送信、攻撃ペイロード送信
- 能力: 通報フォームへの連打、巨大ファイル投擲
- 防御:
  - PoW (hashcash 型) で 1 通報あたり数秒の計算コスト
  - 1 通報あたりサイズ上限
  - TTL による自動削除
  - 添付ファイルはサーバ側で開かない (受信者がローカルで復号する設計)

## 3. スコープ外 (out of scope)

明示的に守らないもの:

- **グローバル受動観測者**: Tor 自体を破る能力を持つ国家レベル攻撃者
- **通報者の端末がマルウェア感染**: Tor Browser の責務
- **受信者の秘密鍵流出**: 受信者の責任 (USB / 暗号化ボリュームでの保管を推奨)
- **国家レベルの暗号解読**: age (X25519 + ChaCha20-Poly1305) の前提に依存
- **物理サイドチャネル**: 電力解析、TEMPEST、音響解析等
- **SecureDrop 完全互換**: 別物として設計
- **通報の真正性検証**: なりすまし・デマの内容判定は受信者(ジャーナリスト)の仕事

## 4. 信頼ベース (TCB)

これが破られると全体が崩れる:

- Tor (Hidden Service v3 + Client Authorization、vanguards-lite)
- age 暗号 (`filippo.io/age`)
- HKDF-SHA256 + ChaCha20-Poly1305 (返信の対称暗号、`golang.org/x/crypto`)
- argon2id (コード照合 + 受信者パスフレーズ KDF)
- Go 標準ライブラリの `crypto/rand`、`crypto/subtle`
- SQLite (`modernc.org/sqlite`)
- ホスト OS のカーネル / ファイルシステム
- 受信者の age 秘密鍵管理 (オフライン保管)
- 受信者のパスフレーズ管理 (TOTP secret / Tor Client Auth 秘密鍵 を保護する master_key の素材)

## 5. 受け入れる残余リスク

- サーバ運営者は「いつアクセスがあったか」のタイミング情報を観測しうる (timing 正規化で粒度は粗くする)
- 受信者ダッシュボード経由で **通報の存在自体** は攻撃者に見える (中身は暗号化済み、サイズは bucket 表示で粗く)
- 通報コードを失うと通報者は再アクセス不能 (意図的設計、サーバ側に復旧手段を持たせない)
- 受信者が「サーバ上で復号するなというルール」を守らないと A1 に対する防御が崩れる (v1 では受信者端末を信頼する妥協)
- 受信者→通報者の返信暗号化は **コードから派生する対称鍵** に依存。受信者が `/admin/reply` でコードを入力する瞬間、サーバプロセスはコードを一時的にメモリ上で扱う (in-memory zero 化で粒度を最小化するが、process memory dump の窓は存在する)
- v1 では **画像と plain text のみ受信**。PDF/Office/SVG/動画/音声は受信拒否 (これらのメタデータ除去を完全に行う実装が現実的でないため、安全側に倒した結果)
- "Safest" モードの Tor Browser を使う通報者をサポートするため JavaScript を一切使わない。代償として、PoW はサーバ側のみで実装するため、極めて高速な分散攻撃者には抑止力が弱い (Tor サーキット確立のコストが代替の抑止力として働く)

## 6. 非目標

- エンタープライズ SOX / J-SOX 監査要件
- 公益通報者保護法に基づく証跡管理 (内部通報窓口 SaaS は別レイヤで開発)
- 多テナント SaaS 化 (将来別レイヤ)
- 多言語 UI (v1 は日本語 + 英語のみ)
- 大規模組織での運用 (受信者数 >5 は想定外)

## 7. セキュリティ原則

1. **書かない情報は漏れない** — アクセスログ、IP ログ、UserAgent ログ、すべて取らない
2. **平文をディスクに残さない** — メモリ上の処理 → 即 age 暗号化 → blob 書き込み
3. **デフォルトで最強** — オプトインで弱める事はできる、その逆は無い
4. **削減主義** — 「便利機能」を足す前に攻撃面の増加を勘定する
5. **明示 > 暗黙** — 失敗は隠さずクラッシュ、サイレントフォールバック禁止
6. **第三者監査前は experimental** — 命に関わるので「安全っぽい」を許さない
